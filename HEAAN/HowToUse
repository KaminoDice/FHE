step 1. put the .h files and libHEAAN.a and the source file you want to compile in the same directory.

step 2. # g++ run.cpp -o HEAAN libHEAAN.a -lntl -lgmp -lm -std=c++11 -o2 -pthread

step 3. # ./HEAAN
